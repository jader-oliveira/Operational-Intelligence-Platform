# Archived: original component specifications

These specs were generated from a common template (identical boilerplate sections
across all 10 components) and describe a 17-service architecture that was superseded
by the MVP rebuild (see `docs/00-executive/mvp-rebuild-plan.md` and ADRs 0013–0017).

Kept for reference only. Real component specs will be written per component as each
one is actually built: collector, core, agent-toolsets.
